<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<comment/>
	<entry key="user-croplist">
        SELECT
        	   UF.USER_NO
        	 , CR.CROP_NAME
        	 , RF.CROP_AMOUNT
        	 , UF.COIN
          FROM USER_INFO UF, RETAIN_CROP RF, CROP CR
         WHERE UF.USER_NO = RF.USER_NO
           AND CR.CROP_ID = RF.CROP_ID
           
	</entry>
	<entry key= "cropPrice">
		SELECT
			   CROP_PRICE
		  FROM CROP
		 WHERE CROP_ID = 1
	</entry>
	<entry key= "updateCropAmount">
		UPDATE RETAIN_CROP
		   SET CROP_AMOUNT = CROP_AMOUNT - ?
		 WHERE CROP_ID = 1
	
	</entry>
</properties>







